# GraphQL Schema Extension for One-to-One Relationships
# This extends the generated schema to include relationship fields

extend type names {
  record: records
}

extend type records {
  name: names
}
